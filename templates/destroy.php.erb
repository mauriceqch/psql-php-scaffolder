<?php 

if (isset($_GET['id'])) {
  require_once 'db-connect.php';

  $result = pg_delete($dbconn, "<%= @table_name %>", array("id" => $_GET['id']));
  if (!$result) {
      echo "An error occurred.\n";
  } else {
      echo "Deleted successfully.\n";
  }
  pg_close($dbconn);

}
?>
